name: Deployment S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "16"

      - name: Install AWS CLI
        run: |
          sudo apt-get install -y awscli
        # Ensure AWS CLI is installed and available
        env:
          aws-access-key-id: "AKIA6FLQ2HYG2X5DGZVK"
          aws-secret-access-key: "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"
          aws-region: us-east-1

      - name: Deploy to S3
        run: |
           export AWS_EC2_METADATA_DISABLED=true
           aws s3 rm s3://testbucketsq --recursive --region us-east-1 --debug
          
        env:
          aws-access-key-id: "AKIA6FLQ2HYG2X5DGZVK"
          aws-secret-access-key: "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"
          aws-region: us-east-1
